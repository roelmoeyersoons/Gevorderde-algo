{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "cpp build and run main",
            "command": "g++ -std=c++17 -g ${workspaceFolder}/main.cpp && ./a.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}